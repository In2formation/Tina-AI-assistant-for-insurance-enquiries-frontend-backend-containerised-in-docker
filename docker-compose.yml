
services:
  backend:
    build: ./Backend
    container_name: backend
    ports:
      - "3005:3000"   # host:container
    environment:
      - NODE_ENV=production

  frontend:
    build: ./Frontend
    container_name: frontend
    ports:
      - "3001:80"
    depends_on:
      - backend


